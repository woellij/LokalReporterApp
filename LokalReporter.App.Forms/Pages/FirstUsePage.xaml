<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms"
             x:Class="LokalReporter.App.FormsApp.Pages.FirstUsePage">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ListView VerticalOptions="Fill" ItemsSource="{Binding DistrictSetting.Districts}" SelectedItem="{Binding DistrictSetting.SelectedDistrict, Mode=TwoWay}" >
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Name}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <Button Grid.Row="1" VerticalOptions="End" HorizontalOptions="Fill"  Text="Weiter" Command="{Binding Continue}" />
  </Grid>
</ContentPage>