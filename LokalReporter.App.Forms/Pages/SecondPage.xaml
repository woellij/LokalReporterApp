<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:Xamarin.Behaviors;assembly=Xamarin.Behaviors"
             x:Class="LokalReporter.App.FormsApp.Pages.SecondPage">
  <ListView ItemsSource="{Binding Articles}" ItemTapped="ListView_OnItemTapped">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ImageCell Text="{Binding Title}" Detail="{Binding HtmlContent.InnerHtml}" ImageSource="{Binding Images[0].Source}"  />
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>