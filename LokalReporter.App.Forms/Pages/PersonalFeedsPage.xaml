<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls1="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms"
             xmlns:views="clr-namespace:LokalReporter.App.FormsApp.Views;assembly=LokalReporter.App.FormsApp"
             xmlns:viewModels="clr-namespace:LokalReporter.App.FormsApp.ViewModels;assembly=LokalReporter.App.FormsApp"
             x:Class="LokalReporter.App.FormsApp.Pages.PersonalFeedsPage"
             Title="{Binding Title}">

  <ScrollView >
    <StackLayout>
      <StackLayout IsVisible="{Binding BookmarksViewModel.HasItems}">
        <Label FontSize="Large" Text="Ihre Leseliste" />
        <views:SimpleFeed ItemsSource="{Binding BookmarksViewModel.BookmarkedArticles}" ItemClickCommand="{Binding ShowDetails}" />
      </StackLayout>
      <controls1:RepeaterView x:TypeArguments="viewModels:FeedViewModel" ItemsSource="{Binding Feeds}">
        <controls1:RepeaterView.ItemTemplate>
          <DataTemplate>
            <views:FeedView />
          </DataTemplate>
        </controls1:RepeaterView.ItemTemplate>
      </controls1:RepeaterView>
      <views:AddFilterView Command="{Binding AddNewFeedFilter}"/>
    </StackLayout>
  </ScrollView>
</ContentPage>